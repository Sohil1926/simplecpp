#include <vector>

using namespace std;

void display_misses(int misses);
void greet();
void end_game(string answer, string codeword);
void display_status(vector < char > inccorect, string answer);
